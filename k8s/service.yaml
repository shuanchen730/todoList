apiVersion: v1
kind: Service
metadata:
    name: my-service
spec:
    selector:
      app: webserver
    type: NodePort
    ports:
      - protocol: TCP
        port: 3388
        targetPort: 3388
        nodePort: 30000

